# Agent 工作入口

先確認 `pwd`、`git rev-parse --show-toplevel` 與 `git status --short`。不要假設目前副本就是使用中的網站，也不要執行本機 `.cmd-queue`。

## 最小閱讀順序

1. 使用者本次指示優先；讀 [README](README.md) 理解工具，再讀 [整理狀態](90_系統/整理狀態.md) 確認邊界。
2. 只按任務補充下表文件，不整批掃描工作歷史、備份、套件或封存。

| 任務 | 優先入口 |
| --- | --- |
| 安裝、環境、啟動或交付 | [啟動與驗證](90_系統/維護說明/啟動與驗證.md) |
| 工作、稿件、影片或重跑路徑 | [工作與影片位置](90_系統/維護說明/工作與影片位置.md)、`90_系統/工作儲存.js` |
| OCR、配圖與人工框選 | [OCR與配圖設定](90_系統/維護說明/OCR與配圖設定.md) |
| 版型與品牌素材 | [模板導覽](02_影片模板/README.md)，再進相關模板實作 |
| 網頁介面 | `90_系統/應用程式/server/public/` |
| 服務、資料或產線 | [系統導覽](90_系統/README.md) |

## 不可破壞的契約

- 程式根為 `90_系統/應用程式/`，資料根是 repository 根；路徑經由 `90_系統/paths.js` 與 `工作儲存.js` 解析。
- 每筆 job 使用完整 ID。工作第一層放 MP4、稿件，另有素材與 `_製作資料/`；不得憑同標題或短 ID 合併、猜測版本或用共用暫存影片替代缺件。
- 成功、失敗、取消、待審核與缺件工作都是歷史，不依年代自動刪除。修正資料路徑須同時核對讀取、建立、上傳、備份、重跑及下載。
- 保留稿件 `===` 分隔、發音替換、`(imageN)`／`(logo)`／`(text:)` 等 marker 契約。修改產生邏輯，不直接改使用者生成的 JSON。
- `init` 只補缺檔；它的空結構供首次載入與檢查，不能當作成品或真實工作樣本。現有詞庫、設定、字幕與工作資料不能覆寫。
- 保留 `.run.lock`、透明圖片及原有模板／OCR 行為；本輪需求之外不切換模型、引擎或渲染規格。
- 不輸出憑證、不把 `.env`、工作資料、封存、記憶或留言加入 Git。新增依賴或文件時檢查公開收錄範圍。

## 工作與驗證

在確認過的根目錄執行 `npm run setup`、`npm run init`；既有環境只在需要時執行。用 `npm run doctor` 檢查本機條件。

- 一般程式變更：先跑相關測試；交付前執行 `npm run verify`。
- 文件修改：檢查相對連結與 `git diff --check`，不為文字調整重跑整條產線。
- 提交／推送前：`npm run check:release` 檢查實際追蹤清單，另審閱 diff 與新檔；忽略規則不會自動停止追蹤舊檔。
- `verify` 使用合成資料、暫存打包與臨時本機 HTTP 服務，不讀取正式工作或呼叫付費 API。
- `npm run studio` 會載入並更新該副本的工作狀態；不能用正式工作台作無副作用測試。出片、HeyGen／MiniMax、服務切換與遠端操作須符合使用者授權。

完成後更新簡短的有效決策與未完成事項，不新增逐次對話日誌。一次性交接文件必須寫明用途、CWD 與失效條件；失效後從現行入口移除。`99_封存/` 不構成現行規則或預設 context。
